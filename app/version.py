"""Versao do aplicativo.

    Fonte da verdade em Python. O `instalador.iss` (`AppVersion`) e o
    `version_info.txt` (`FileVersion`) continuam mantidos a mao, porque cada um e
    lido por uma ferramenta diferente - sao tres lugares.

    Formato: so numeros e pontos (MAJOR.MINOR.PATCH).
    """

__version__ = "1.0.0"
